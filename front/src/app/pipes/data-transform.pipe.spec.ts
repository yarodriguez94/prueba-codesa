import { DataTransformPipe } from './data-transform.pipe';

describe('DataTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new DataTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
